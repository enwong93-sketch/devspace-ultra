interface:
  display_name: "DevSpace Network Setup"
  short_description: "Install and verify DuckDNS/DDNS or Cloudflare ingress"
  default_prompt: "Set up or repair DevSpace Ultra. Prefer DuckDNS/DDNS plus Caddy, execute safe local commands, guide only unavoidable user account/router actions, and verify the real public MCP/OAuth path."
policy:
  allow_implicit_invocation: true
